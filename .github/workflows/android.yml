name: Android

on:
  push:
    branches: ["**"]
  pull_request:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Tests und APK
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: JDK 17 einrichten
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Gradle einrichten
        uses: gradle/actions/setup-gradle@v4

      - name: Android SDK bereitstellen
        run: |
          SDKMANAGER="$ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager"
          yes | "$SDKMANAGER" --licenses > /dev/null 2>&1 || true
          "$SDKMANAGER" "platform-tools" "platforms;android-35" "build-tools;35.0.0"

      - name: Spiellogik testen
        run: |
          set +e
          ./gradlew :core:test --no-daemon --console=plain > build.log 2>&1
          code=$?
          # Kotlin's own error lines first — they are what a failure here almost always is.
          grep -E "^e: |^\\* What went wrong|^Caused by|FAILURE:" build.log | head -n 40
          echo "--- letzte Zeilen ---"
          tail -n 20 build.log
          exit $code

      - name: Debug-APK bauen
        run: |
          set +e
          ./gradlew :app:assembleDebug --no-daemon --console=plain > build.log 2>&1
          code=$?
          # Kotlin's own error lines first — they are what a failure here almost always is.
          grep -E "^e: |^\\* What went wrong|^Caused by|FAILURE:" build.log | head -n 40
          echo "--- letzte Zeilen ---"
          tail -n 20 build.log
          exit $code

      - name: Release-APK bauen (unsigniert, prüft R8)
        run: |
          set +e
          ./gradlew :app:assembleRelease --no-daemon --console=plain > build.log 2>&1
          code=$?
          # Kotlin's own error lines first — they are what a failure here almost always is.
          grep -E "^e: |^\\* What went wrong|^Caused by|FAILURE:" build.log | head -n 40
          echo "--- letzte Zeilen ---"
          tail -n 20 build.log
          exit $code

      - name: APK hochladen
        uses: actions/upload-artifact@v4
        with:
          name: kollaps-debug-apk
          path: app/build/outputs/apk/debug/*.apk
          if-no-files-found: error

      - name: Testbericht hochladen
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: core-test-report
          path: core/build/reports/tests/test
          if-no-files-found: ignore

  screenshots:
    name: Bildschirme aus der Desktop-Fassung
    runs-on: ubuntu-latest

    # The harness compiles the app's own interface sources against Compose Multiplatform and
    # renders them without a display, so every run leaves behind pictures of the actual screen.
    # That is the only automated look at the interface there is — nothing else in this pipeline
    # ever draws it.
    steps:
      - uses: actions/checkout@v4

      - name: JDK 17 einrichten
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Gradle einrichten
        uses: gradle/actions/setup-gradle@v4

      - name: Bildschirme rendern
        run: |
          set +e
          ./gradlew --configure-on-demand --no-daemon --console=plain \
            :desktop:run -PmainClass=com.staatseigentum.kollaps.desktop.ScreenshotsKt \
            --args="build/screenshots" > build.log 2>&1
          code=$?
          grep -E "^e: |^\\* What went wrong|^Caused by|Exception|FAILURE:" build.log | head -n 40
          echo "--- letzte Zeilen ---"
          tail -n 20 build.log
          exit $code

      - name: Bildschirme hochladen
        uses: actions/upload-artifact@v4
        with:
          name: kollaps-bildschirme
          path: build/screenshots/*.png
          if-no-files-found: error
